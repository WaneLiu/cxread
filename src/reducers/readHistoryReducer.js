import {ADD_READ_HISTORY, MODIFY_READ_HISTORY} from '../modules/constants/actionTypes'

const addReadHistory = (state, action) => {
    switch (action.type) {

    }
}

const modifyReadHistory = (state, action) => {
    switch (action.type) {
        
    }
}